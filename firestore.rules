rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 誰でも読み書きできてしまう状態から、ログインしているユーザーのみに変更
      allow read, write: if request.auth != null;
    }
  }
}
